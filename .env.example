# =============================================================================
# SIMS Environment Variables
# Copy this file to .env and fill in your values:  cp .env.example .env
# =============================================================================

# --- Required ----------------------------------------------------------------
# Supabase project URL (from Supabase Dashboard > Settings > API)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous/public key (from Supabase Dashboard > Settings > API)
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-key-here

# --- Optional ----------------------------------------------------------------
# Sentry DSN for error tracking (leave empty to disable)
VITE_SENTRY_DSN=

# App version for Sentry release tracking
VITE_APP_VERSION=1.0.0

# Force-enable Sentry in non-production environments (set to "true" to enable)
VITE_SENTRY_ENABLED=

# --- Edge Functions (set via Supabase CLI, not in this file) -----------------
# These are configured with:  supabase secrets set KEY=value
#
# RESEND_API_KEY     - Resend API key for transactional email
# FROM_EMAIL         - Sender address (e.g. "SIMS <notifications@yourdomain.com>")
#
# These are automatically available in Edge Functions (no config needed):
# SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY
